server:
  port: 8890
  host: http://localhost:8890/

spring:
  redis:
    host: 127.0.0.1
    port: 6379
    database: 0
    password: root
  datasource:
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/url_test?characterEncoding=utf-8&serverTimezone=Asia/Shanghai

mybatis-plus:
  configuration:
    cache-enabled: false
    map-underscore-to-camel-case: true
  mapper-locations: classpath:mapper/**/*Mapper.xml
  global-config:
    banner: false
    db-config:
      table-underline: true

